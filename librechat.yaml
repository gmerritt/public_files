# For more information, see the Configuration Guide:
# https://www.librechat.ai/docs/configuration/librechat_yaml

# Configuration version (required)
version: 1.2.1

# Cache settings: Set to true to enable caching
cache: true

interface:
  privacyPolicy:
    externalUrl: "https://example.com/privacy"
    openNewTab: true
  termsOfService:
    externalUrl: "mailto:riverflow@berkeley.edu"
    openNewTab: true
    modalAcceptance: true
    modalTitle: "Send feedback to Bill and Greg"
    modalContent: |
      # River
      ## Early-alpha trial
      You may send feedback to Bill and Greg at: riverflow@berkeley.edu
